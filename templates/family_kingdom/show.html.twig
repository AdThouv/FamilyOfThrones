{% extends 'base.html.twig' %}

{% block title %}FamilyKingdom{% endblock %}

{% block body %}
    <h1>FamilyKingdom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family_kingdom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ family_kingdom.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ family_kingdom.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_kingdom_index') }}">back to list</a>

    <a href="{{ path('family_kingdom_edit', {'id': family_kingdom.id}) }}">edit</a>

    {{ include('family_kingdom/_delete_form.html.twig') }}
{% endblock %}
